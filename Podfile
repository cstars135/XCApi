platform :ios, '8.0'

use_frameworks!

target 'XCApi' do
    pod 'ReactiveSwift', '~> 1.0.0'
    pod 'Alamofire', '~> 4.4'
    pod 'SwiftyJSON', '~> 3.1.4'

    target 'Tests' do
       inherit! :search_paths
       pod 'Quick'
       pod 'Nimble'
    end
end
